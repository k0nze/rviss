#!/bin/sh

case "$1" in
  start)
    echo "Running Harbor mmio-test dry-run"
    /usr/bin/mmio-test --dry-run 0x10010000
    ;;
  stop|restart|reload)
    ;;
  *)
    echo "Usage: $0 {start|stop|restart|reload}"
    exit 1
    ;;
esac

exit 0
